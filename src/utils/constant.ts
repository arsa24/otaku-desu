import { fetch } from "./fetch";

export const URL = "https://otakudesu.cloud/";

// export const URL = async (): Promise<string> => {
//   const $ = await fetch(BASE_URL);
//   const link = $("a#skip").attr("href");
//   if (!link) throw new Error("Link not found");
//   return link;
// };
